class SliderConfigs {
  static const int firstIndex = 2;
  static const int lastIndex = 9;

  static const double topPadding = 15;
  static const double bottomPadding = 11;

  static final List<int> list = List.generate(10, (index) => index * 10);
}
